{
  "tasks": {
    "lume": "echo \"import 'lume/cli.ts'\" | deno run --unstable -A -",
    "build": "make -C ../spec && deno task lume",
    "serve": "make -C ../spec && deno task lume -s"
  },
  "imports": {
    "lume/": "https://deno.land/x/lume@v3.2.1/"
  }
}
